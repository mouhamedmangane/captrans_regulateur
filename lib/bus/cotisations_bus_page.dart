import 'package:flutter/material.dart';

class CotisationBusPage extends StatelessWidget {
  const CotisationBusPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class CotisationBusView extends StatelessWidget {
  const CotisationBusView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

